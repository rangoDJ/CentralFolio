# Optional server configuration
# All portfolio credentials (SnapTrade keys, API keys, etc.) are managed
# through the web UI under Settings — no environment variables needed.

PORT=3000
LOG_LEVEL=info
