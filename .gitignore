node_modules/
dist/
.env
user-credentials.json
*.log
.DS_Store

# SQLite database + WAL/SHM sidecars (may contain secrets — never commit)
*.db
*.db-shm
*.db-wal

# Scratch / inspection dirs
tmp_*/

# Database backups / salvaged copies (same secrets as the live DB)
backup_db_*/
*.db.CORRUPT
